import { MapPin, LocateFixed, Clock } from "lucide-react";

interface CardMapComponentProps {
  origin: string;
  destination: string;
  departureTime: string;
}

const CardMapComponent = ({ origin, destination, departureTime }: CardMapComponentProps) => {
  // Este componente está disponible pero no se usa en la pantalla de detalles
  // Se mantiene para futuro uso
  return null;
};

export default CardMapComponent;
